import React from 'react';
import ClassManager from './ClassManager';

/**
 * ViewClasses Component
 * 
 * This component serves as a simple wrapper for the ClassManager component.
 * It renders ClassManager directly without adding any additional logic or props.
 */
const ViewClasses = () => {
  return <ClassManager />;
};

export default ViewClasses;
